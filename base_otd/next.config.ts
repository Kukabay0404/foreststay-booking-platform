import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ["mir-s3-cdn-cf.behance.net", "static.tildacdn.com", "i.pinimg.com", "st.hzcdn.com", "elles.top"],
  },
};

export default nextConfig;
